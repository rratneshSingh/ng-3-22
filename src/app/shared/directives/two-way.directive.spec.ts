import { TwoWayDirective } from './two-way.directive';

describe('TwoWayDirective', () => {
  it('should create an instance', () => {
    const directive = new TwoWayDirective();
    expect(directive).toBeTruthy();
  });
});
